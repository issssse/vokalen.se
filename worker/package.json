{
  "name": "vokalen-admin-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-assets": "bash ./scripts/sync-assets.sh",
    "dev": "npm run sync-assets && wrangler dev --port 8787",
    "deploy": "npm run sync-assets && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.13.2"
  }
}
